# TrustedShopsE2ETest
Test Case Task for Trusted Shops Interview
Prerequisites:
Java
TestNG framework
Selenium WebDriver
An IDE such as Eclipse

Steps to execute the test:
Clone the repository from Github, containing the test code onto your local machine.
Open the project in your IDE.
Navigate to the "src/main/java" directory and locate the "TrustedShopsE2ETest.java" file.
Right-click on the file and select "Run As" > "TestNG Test".
The test will start executing and you will be able to see the progress and results in the console.
